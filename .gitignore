__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Local database — regenerate with `make demo`, never commit.
*.db
*.db-wal
*.db-shm

# Generated fixtures. The SEEDS are committed (in code); the CSVs are not.
fixtures/*/*.csv

# LLM response cache. Commit only the small fixture cache CI needs.
.llm_cache/

ui/node_modules/
ui/dist/
.DS_Store
.idea/
.vscode/

# Hypothesis example database and constants cache — machine-local, regenerated by pytest.
.hypothesis/
